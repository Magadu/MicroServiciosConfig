spring:
  application:
    name: microservice-cervezas
  cloud:
    config:
      name: ${spring.application.name}
      uri: http://localhost:8888
#      fail-fast: true
#      password: ${CONFIG_SERVICE_PASSWORD}
#      username: user
