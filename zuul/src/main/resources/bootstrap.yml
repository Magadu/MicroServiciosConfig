spring:
  application:
    name: zuul
  cloud:
    config:
      name: ${spring.application.name}
      uri: http://localhost:8888
#      fail-fast: false
#      password: ${CONFIG_SERVICE_PASSWORD}
#      username: user
